{
  "name": "feijoa-ui-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd ./site && bun run dev",
    "build:site": "cd ./site && bun run build",
    "build:lib": "cd ./packages/feijoa-ui && bun run build",
    "build:cli": "cd ./packages/cli && bun run build"
  }
}
